# Azure DevOps Configuration
# Copy this file to .env and fill in your actual values

# Your Azure DevOps organization name (just the name, not the full URL)
# Example: if your ADO URL is https://dev.azure.com/mycompany, use 'mycompany'
ADO_ORGANIZATION=your-organization-name

# Your Azure DevOps Personal Access Token
# Generate this from: Azure DevOps → User Settings → Personal Access Tokens
# Required scopes: Work Items (Read & Write), Code (Read), Build (Read), Project and Team (Read)
ADO_PAT=your-personal-access-token-here

# Your default ADO project name (optional - can be overridden in app settings)
# Example: if your project URL is https://dev.azure.com/mycompany/MyProject, use 'MyProject'
ADO_PROJECT=your-project-name

# Security Note:
# - Never commit your actual .env file to git
# - The .env file should be in your .gitignore
# - These credentials are processed securely by Tauri's Rust backend
# - PAT tokens are never exposed to the browser frontend